#include <stdio.h>

/**
 * main - program that prints the number of arguments
 * @argc: arg counter
 * @argv: string
 * Return: 0 success
 */
int main(int argc)
{
	printf("%d\n", argc - 1);

	return (0);
}
