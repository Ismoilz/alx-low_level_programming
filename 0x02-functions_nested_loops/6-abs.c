#include "main.h"

/**
 * _abs - Gives the absolute value of an int
 * @n: value to check
 * Return: the absolute value of the number
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
